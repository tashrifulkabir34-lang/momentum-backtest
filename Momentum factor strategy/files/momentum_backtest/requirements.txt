# Momentum Factor Strategy Backtest
# Python 3.9+ required
#
# Core scientific stack
numpy>=1.24.0
pandas>=1.5.0
scipy>=1.10.0
matplotlib>=3.6.0
seaborn>=0.12.0

# Optional — install when network is available for live data
# yfinance>=0.2.28          # Live S&P 500 price data
# quantstats>=0.0.62        # Extended tearsheet (HTML output)
# zipline-reloaded>=3.0.0   # Event-driven backtesting engine

# Testing
pytest>=7.0.0
